info:
  name: register admin
  type: http
  seq: 7

http:
  method: POST
  url: "{{actual_url}}/auth/admin"
  body:
    type: json
    data: |-
      {
        "username": "admin2",
        "password": "password",
        "email": "admin2@example.com",
        "first_name": "Admin",
        "last_name": "Huancayo"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
