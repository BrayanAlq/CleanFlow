info:
  name: register admin
  type: http
  seq: 7

http:
  method: POST
  url: "{{actual_url}}/auth/admin"
  body:
    type: json
    data: |-
      {
        "username": "admin1",
        "password": "password",
        "email": "admin1@example.com",
        "first_name": "Admin",
        "last_name": "Lima"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
