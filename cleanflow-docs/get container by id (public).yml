info:
  name: get container by id (public)
  type: http
  seq: 14

http:
  method: GET
  url: "{{actual_url}}/container/device"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
