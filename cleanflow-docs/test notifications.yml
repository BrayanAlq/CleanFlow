info:
  name: test notifications
  type: http
  seq: 29

http:
  method: GET
  url: "{{actual_url}}/test-notify/10"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
