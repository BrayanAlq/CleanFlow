info:
  name: register driver
  type: http
  seq: 4

http:
  method: POST
  url: "{{actual_url}}/auth/driver"
  body:
    type: json
    data: |-
      {
        "username": "driver5",
        "password": "password",
        "email": "driver5@example.com",
        "first_name": "Alma",
        "last_name": "Driver"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
