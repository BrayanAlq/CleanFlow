info:
  name: get my scheduled route (driver)
  type: http
  seq: 25

http:
  method: GET
  url: "{{actual_url}}/driver/scheduled-route"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
