info:
  name: get all containers in viewport
  type: http
  seq: 10

http:
  method: GET
  url: "{{actual_url}}/container/viewport"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
