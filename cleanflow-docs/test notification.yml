info:
  name: test notification
  type: http
  seq: 29

http:
  method: POST
  url: "{{actual_url}}/test-notify/resident4"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
